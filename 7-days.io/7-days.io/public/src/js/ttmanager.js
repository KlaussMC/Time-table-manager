const ttmanager = () => { document.querySelector("#editorField").style.display = "block"; }

let data = [];

const editField = (field) => { let dat = {}; /* get day and period */ data.push(getInfo(dat)) }

const getInfo = (field) => {
  let dat = {
    day: 
  }
}